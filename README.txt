To compile the code simply run make.

If on a new machine run make clean first to make sure it is compiled properly.

To run the code with the input file flow.txt simply type: ./MFS flow.txt
into the terminal and it should run properly
